components:
  schemas:
    MediaStatsSummaryResponse:
      type: object
      properties:
        totalMedia:
          type: integer
          format: int64
        planned:
          type: integer
          format: int64
        inProgress:
          type: integer
          format: int64
        completed:
          type: integer
          format: int64
        dropped:
          type: integer
          format: int64
        averageRating:
          type: number
          format: double